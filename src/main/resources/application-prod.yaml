app:
  version: "@project.version@"
vep:
  port: ${MYSQL_PORT}
  forks: 4
  host: ${MYSQL_HOST}
  username: ${MYSQL_USER}
  password: ${MYSQL_PASSWORD}
  hgvs-max-threads: 75
  data-source-mode: ${VEP_DATA_SOURCE_MODE:database}
  polyphen-sift-filename:
  alpha-missense-filename:
